sequence = [1, 2, 3, 4];

// for loop
for i = 0; i < sequence->len(); i = i + 1 {
    print(i);
}

// foreach loop
foreach idx vl in sequence {
    print([idx, vl]);
}

// while loop
while sequence->len() {
    print(sequence.pop());
}
